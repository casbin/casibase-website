{
    "ignorePatterns": [
        {
            "pattern": "^(?!https?:\/\/)"
        },
        {
            "pattern": "^http://localhost"
        },
        {
            "pattern": "^https://azure.microsoft.com/en-us/products/storage/blobs"
        },
        {
            "pattern": "^https://[\\w.]*openai.com"
        }
    ]
}